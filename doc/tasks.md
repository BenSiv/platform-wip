# Task tracker

Snapshot of platform-wip's open/closed work items, kept in the repo so
task state survives across sessions/tools (the interactive task list
used during development is session-scoped and does not itself persist).
Update this file when tasks are added, completed, or reprioritized.

## Pending

Reprioritized 2026-07-21: explicit user direction to focus on cleanup/maintainability/coherent-
interfaces work before any new features. The "Cleanup tier" below runs top to bottom first, in
that order (user-confirmed); the "New-feature tier" underneath is deliberately held back until
the cleanup tier is done, not dropped. #74/#75/#76 (SQLite-specific durability work) stayed
dropped after #82's MariaDB cutover -- Cloud SQL owns its own backup/PITR/failover now.

Re-reprioritized 2026-07-21 (same day, follow-up direction): within the cleanup tier, quick
self-contained wins go first, ahead of items that need a design/architecture decision before any
work can start. New order: #101, #94, then #85/#89 (both genuinely blocked on a decision --
rootful-vs-rootless for #85, a scoping conversation for #89 -- not quick by nature, not
deprioritized by preference).

Cleanup tier is empty as of 2026-07-22 (#89 done, #85 deferred with a note -- not dropped). Next
priority is either reviving #85 or moving into the new-feature tier below; #95/#96 remain
actionable any time by whoever has GCP Org/Billing Account admin access.

### Cleanup tier (working through this first, in order)

_Empty -- see note above._

### Blocked / contingent on humans (unaffected by the cleanup-first reordering)

| # | Subject | Notes |
|---|---------|-------|
| 95 | Confirm the Google Cloud Data Processing Addendum (CDPA) is accepted for this project | Cheap to check (for whoever has GCP Org/Billing Account admin access), real legal/contractual gap (not a design one). No `gcloud` CLI surface exists to check/accept it; I can't do this myself. See `~/software/docs/ai-agent-and-lab-data-compliance-research.md`. |
| 96 | Confirm/obtain a Google Cloud HIPAA BAA, if PHI is ever in scope | Same console location as #95, separate "Review and Accept" action. Contingent -- needs a deliberate "is PHI ever in scope" yes/no first, rather than being actionable on its own. |

### New-feature tier (deliberately held back until the cleanup tier above is done)

| # | Subject | Notes |
|---|---------|-------|
| 84 | Design multivalue field support for entity schemas | **Next up.** From user notes: "how do we record multivalue data entry in a relational database." `schema.lua`'s `FIELD_TYPES` (text/number/date/select/reference) has no array/multivalue type today -- e.g. an entity needing several lot numbers or several linked reagents has no first-class way to express that. Real architecture question, not yet scoped: a join table per multivalue field? A JSON-array column with app-level validation? Affects schema.md and the projected-table generation model. |
| 88 | Design separation between science-analysis data and operational/audit data | From user notes: "raw data schema vs operational data schema separation" -- corrected: NOT about Benchling import data, but about the platform's own data. The distinction is between user-entered scientific data meant for analysis (entity/reagent/experiment/sample rows, etc.) and operational data whose purpose is auditing (the `entity_event` ledger, user/session/admin activity). Today both live in one undifferentiated schema/storage model -- worth designing whether they should be separated (storage location, access patterns, retention/backup policy) given their different purposes. |
| 73 | Add label printing support (ZPL templates per entity schema) | New, larger feature. Port Benchling's workflow: per-schema ZPL label templates, a "Print label" action on any entity, template-embedded SQL to pull entity-specific fields (parameterized/read-only, no injection). Printing likely goes through **Zebra Browser Print** specifically (a local agent + JS SDK that lets a web page discover/print to a Zebra printer directly from the browser, no server-side print-queue plumbing needed) rather than a generic "printer software connection." Needs design: where templates live (schemas dir vs. new template type, matching the `views/`/`templates/` convention), safe SQL substitution, and the Browser Print JS integration. |
| 98b | Schema-change-recording parity for the Benchling-sync Cloud Run job | Harder half of #98. `datapipe/main.py` calls `record_schema_change()` (persisted schema-diff records under `database/schema/versioning/changes/`, tied to a `--schema-change-policy` of fail/rebuild/ignore), using Warehouse-based schema introspection (`benchling/schema/schema_spec.py`'s `schema_spec()`, which uses `ApiUtils`/a personal API key, not the REST/OAuth2 app credentials `benchling/sync/main.py` deliberately switched to per its own docstring). The cron job doesn't call this at all today, so schema drift isn't recorded anywhere for the automated path. Closing this gap means either reintroducing Warehouse/personal-API-key access into the Cloud Run job (exactly what the REST migration was meant to avoid) or building a REST-based schema-diff equivalent that doesn't exist yet -- a real design choice, not a small patch. |
| 86 | Replace `/data`'s connection graph with a real ERD diagram | From user notes: "add real erd diagram instead of just a connection graph." Today's `/data` diagram is a plain node-per-type/edge-per-reference graph (see `cgi.bats`'s own test description) -- a real ERD would show field names/types and cardinality, not just connectivity. |
| 108 | Post-chat-turn processing queue + a new, frequent periodic job | From this session's follow-up discussion on task #87, explicit user direction ("queue after response sounds better than nothing"). This app has no in-process async/background-job capability at all -- it's CGI-per-request, one OS process per HTTP request, exiting once the response is sent. Confirmed directly, in answer to "can we use Luam coroutines": no -- a coroutine is a cooperative, single-process control-flow construct that dies with the process; it doesn't outlive the request the way true background/async work would need to. The only "separate process" precedent anywhere in this system is the Benchling-sync systemd timer -- external, independently scheduled, not spawned by request-serving code (and deliberately so; even a detached `os.execute("... &")` child process would be a materially bigger, unsupervised-process operational surface nothing here does today). Instead: queue the work at the end of a chat turn rather than trying to run it inline or truly async -- a new `knowledge_processing_queue` table, written to (cheap, synchronous, just an insert) at the end of `agent.run_turn`, drained by a new systemd timer. Needs its own cadence, much tighter than Benchling sync's once-nightly schedule to feel responsive -- every 1-5 minutes, not reusing that timer. This is the actual trigger mechanism for #107's distillation work (and a natural home for other deferred-processing ideas later, e.g. running heat decay/review as a real batch pass instead of only lazily at read time). |
| -- | Agent-assisted linking (deferred, unscoped) | Follow-up idea from the user during #106/#107 ("perhaps the agent can help also with connecting pieces of information"): have the agent actively notice and propose connections between related documents (new `document_link` rows), not just passively benefit from links a user already wrote, or passively score the existing link graph (task #106's spreading activation). Real new write surface, almost certainly a destructive, approval-gated `AGENT_TOOLS` entry (same bar as `knowledge.distill`/`document.create`) -- needs its own scoping (what counts as "related enough to link," how a proposed link is reviewed/shown) rather than being bolted on ad hoc. No number assigned yet -- not concretely scoped. |

### Deferred/unranked (standing, user's own earlier calls)

| # | Subject | Notes |
|---|---------|-------|
| 85 | Migrate platform deployment from Docker to Podman | Deferred 2026-07-22 after scoping both the rootful-vs-rootless question and the "should this happen at all" question against the real `startup-platform.sh.tpl`. Real, legitimate hardening (removes `dockerd`'s persistent root daemon -- a genuine attack-surface reduction that holds even under rootful Podman), and the build pipeline is unaffected either way (Cloud Build still produces standard OCI images; only the VM runtime would change). Not urgent: no Docker-daemon-related incident has ever occurred here, and several stronger first-order controls already exist (Cloud SQL has zero public network exposure, SSH is IAP-tunnel-only, secrets live in Secret Manager, `deletion_protection` is on) -- this would be a second-order/defense-in-depth control, not a perimeter one. Two real costs found, not hypothetical: (1) `restart: always` durability across a host reboot works today only because `dockerd` is itself a systemd-managed service that restarts containers on its own restart -- Podman has no equivalent persistent process, so this needs an explicit replacement (Podman's own answer is Quadlet, systemd unit files per container/pod), not a CLI swap; (2) the Cloud SQL Auth Proxy sidecar's compose-network setup (`env_file`, service-name resolution, bind-mounted `/opt/platform/data`) has never been run under `podman compose` and needs verifying on a throwaway instance before ever touching `fossci-app-prod`. If picked back up: stage on a disposable VM first, design the Quadlet units up front, and verify the sidecar end-to-end -- the same phased/verify-before-cutover rigor task #82's MariaDB migration used, not a live edit of the production startup script. |
| 53 | Add attachment support to the chat widget | Deferred, future. |
| 57 | Investigate persistent-process mode to replace CGI-per-request | Deferred, future; would remove the per-request CGI bootstrap cost. |
| 65 | Add @mention support to the editor | Deferred, future; user-expressed interest. |

## Completed (most recent first)

- 107: Real agent-driven distillation. Adds `knowledge.distill_document`/`AGENT_TOOLS.knowledge.distill` -- a destructive tool (same approval-gated bar as `document.create`/`entity.create`) that writes a genuinely new, concise, single-idea document extracted from a source the agent has actually read (via the existing `entity.get` tool), replacing what the old (task #106-removed) `materialize_note` pass never actually did -- it only ever promoted a tier number, never synthesized anything. A distilled document always starts at tier 0 like any other new pool document (no pre-promotion just for being agent-written), filed under the Knowledge Pool folder via the existing `knowledge.create_document_note` path, tagged `source_type = 'distilled'` pointing back at its source. `knowledge.list`'s tool output now also surfaces each document's `atomicity_status` (`ok`/`thin`/`needs-split`, `document.atomicity_status`) so the model has a real signal for what's actually worth distilling from -- an already-atomic document has nothing left to extract, and the new `KNOWLEDGE_DISTILL_SYSTEM_PROMPT` explicitly instructs against distilling from one. Triggered explicitly via `platform knowledge distill` (`agent.run_knowledge_distillation`, dispatched from `main.lua` directly rather than `knowledge.do_knowledge` -- same knowledge/agent circular-require reason the old `review` command was) -- task #108's queue is meant to be the actual automated trigger once it exists. New coverage in `tst/integration/knowledge_context.bats`: the full tool-call-to-approval-to-real-document path (a scripted chat turn proposes `knowledge.distill`, nothing is written until `/chat-approve`, then the distilled document exists with the right tier/source_type/source_id and really is filed under the Knowledge Pool folder), plus the CLI trigger's own pending-approval path -- found and fixed a real test-authoring mistake along the way (not a code bug): a raw SQL `INSERT INTO document` for a test fixture collided with `entity.create`'s own ledger-driven id sequence (`UNIQUE constraint failed: document.id`) on the very next real entity creation, since a hand-inserted row bypasses `ledger.append_create`'s auto-increment entirely -- fixed by creating the fixture through the real `entity create` CLI instead. The related idea raised during this same work -- the agent actively *proposing new links* between documents, not just consuming/scoring the existing link graph -- was deliberately left unbuilt and captured as its own unscoped, unnumbered deferred item (real new write surface needing its own approval-gating design, not something to bolt on here). Full suite green (119 bats tests + all unit suites, exit 0).
- 105: Auto-embed documents on create/update, best-effort. `document.create_page`/`update_page` now call `document.reindex_embedding` at the end of every save, not just via the explicit CLI/`_all` bulk path -- one embedding API call per save, not a corpus reindex, cheap in both latency and money as originally scoped. Non-blocking by construction, not by adding new error-swallowing: `agent_provider_vertex.embeddings` already returns `nil, err` gracefully on any failure (missing `VERTEX_PROJECT`, network error, malformed response -- confirmed directly, none of it ever calls Lua's `error()`), and `create_page`/`update_page` simply never check that return value, so a save's own success/failure is completely unaffected either way -- no logging sink exists in this CGI-per-request codebase for a "silent background failure" message (stdout IS the HTTP response), so this is deliberately silent rather than inventing one. `document.reindex_all_embeddings`/the CLI's `reindex-embeddings` command still exist, now repurposed as bulk backfill (documents saved before this landed, or after a provider outage silently dropped some best-effort saves) rather than the only way to populate the cache. `tst/integration/document.bats`'s old test asserting embeddings are explicitly NOT computed on save was rewritten into three: create+update auto-compute (via the deterministic test provider), an unconfigured/failing provider never fails the save itself, and reindex-embeddings still works for backfill. Full suite green (117 bats tests + all unit suites).
- 106: Collapsed `knowledge_note` into `document` -- one unified, tier/heat-scored pool, per explicit user direction ("why do we have a separate note concept? it should all be on the same level but with different scoring based on tier, heat and relevant... put everything into the same table"). `tier`/`heat`/`retrieval_count`/`last_retrieved_at`/`source_type`/`source_id`/`source_ref`/`content_hash`/`duplicate_of`/`merged_into` now live directly on `document` (`document.ensure_document_knowledge_columns`, a real `ALTER TABLE` migration matching `ledger.lua`'s `ensure_entity_event_reason_column` pattern -- not `DOCUMENT_SCHEMA.fields`, which would wrongly expose them as user-editable form fields). A document that's searched now **is** the record that accrues heat/tier directly -- no more shadow row created to track it (`knowledge.ensure_note_for_document`/`note_for_document` deleted outright). System/agent-derived content with no existing page to attach to (today: leaked chat reasoning; future: #107's distilled notes) becomes a genuinely new `document` row instead, filed under a single lazily-created top-level Notebook folder (`document.ensure_knowledge_pool_folder`, "Knowledge Pool") -- per explicit user direction, organized separately from user-authored pages but never hidden, browsable/searchable like any other page. `document.search`'s own blended lexical+embedding scoring now folds in tier/heat reinforcement (`document.tier_weight`/`document.effective_heat`), added only *after* the existing relevance floor so a heavily-reinforced-but-irrelevant document is still excluded, never ranked highly just for being "hot"; documents already `merged_into` a canonical duplicate are excluded from search outright. The pure tier/heat/dedup heuristics (`content_hash`, `effective_heat`, `promotion_target_tier`, `atomicity_status`, `title_is_generic`, `guess_title_from_body`, ...) moved to `document.lua` alongside the columns they score -- `knowledge.lua` depends on `document.lua`, never the reverse, so this avoids a real require cycle. Two review-time mutations are now explicitly gated to system/agent-derived documents only (`source_type` set) -- **retitling** a generic title and **dedup-merging** (`merged_into`) never touch a real user-authored page, even if its title looks generic or its content happens to match another page's, since these are now genuine content/visibility mutations against real user pages, not a hidden tracking record; the review status is still recorded either way for visibility. `knowledge.materialize_note` and its destructive `AGENT_TOOLS.knowledge.materialize` entry, plus the old agent-driven review pass (`agent.run_knowledge_review`, `platform knowledge review`, dispatched specially from `main.lua` to dodge a knowledge/agent require cycle) were removed outright -- their entire reason to exist (promoting a hidden tracking record into a real page) no longer applies once every pool record already is one; real agent-driven distillation is task #107's job, not a revival of this pass. Audit/log tables (`knowledge_retrieval`, `knowledge_retrieval_note` renamed `knowledge_retrieval_document`, `knowledge_review`, `knowledge_context`) had their `note_id`/`reasoning_note_id` columns renamed to `document_id`/`reasoning_document_id` -- these stay separate hand-rolled tables (they're about retrieval/review *events*, not pool content itself) but now reference `document.id` directly. Reasoning-note creation is now attributed to the real logged-in user (`login`, already in scope in `agent.run_turn`), not a synthetic actor -- only the Knowledge Pool folder itself is authored as `"system"`, since nothing else has a natural "current user" at creation time. **Same-session follow-up, explicit user direction** ("I would like to add linking and knowledge graph as part of the computation for context creation"): added ACT-R-style *spreading activation* over the existing `document_link` backlink graph -- when a document is directly retrieved, its linked neighbors (`document.linked_neighbors`, both directions) now get a smaller heat reinforcement too (`knowledge.spread_activation`/`document.spreading_delta`), diluted by fan-out (the "fan effect": a heavily-linked hub gives each neighbor a proportionally smaller nudge, never the full direct-hit amount). Deliberately only `heat`/`last_retrieved_at` move for a spread neighbor, never `retrieval_count` -- that column specifically measures direct hits, since `promotion_target_tier`'s thresholds are defined against it; a neighbor's tier can still rise from the extra heat alone since `review_retrieval` already processes every document touched in a retrieval batch, direct hits and spread neighbors alike, with no extra plumbing needed. A neighbor already a direct hit in the same retrieval is skipped for spreading (it already got the full treatment). The user's related idea -- the agent actively proposing/creating new links between documents it notices, not just scoring links a user already wrote -- was captured as an explicit, not-yet-built design note on task #107 instead of being bolted on here, since it's real new write surface needing its own approval-gating scope. Test suite fully green post-merge (`bld/build.sh` + `bld/test.sh`, 115 bats tests + all unit suites, exit 0) -- `tst/unit/knowledge.lua`'s tier/heat/dedup tests moved to a new `tst/unit/document.lua` (matching where the functions now live) plus a new `tier_weight` accessor test; `tst/integration/knowledge.bats`/`knowledge_context.bats`/`agent.bats` updated for the merged model (no more separate note row per search; `knowledge show`'s "source:" line only appears for system-derived documents; the two materialize/review tests removed outright since that surface no longer exists; a new assertion that a leaked-reasoning document actually lands under the Knowledge Pool folder; a new spreading-activation test confirming a linked-but-not-directly-matched neighbor's heat rises while its retrieval_count stays 0, via the real `/document-save` route since link parsing only runs through `document.create_page`, not the `entity create` CLI shortcut every other test in this file uses). `doc/architecture.md`'s "Knowledge pool" section rewritten to match.
- 104 (found live, real production): the `/sql` ad-hoc console (`view.run_adhoc`) called `sqlite3.open(db_path)` directly, completely bypassing `db.lua`'s own backend dispatch that the whole MariaDB migration was built around -- worked by accident whenever `db_path` was a SQLite file path, but a MariaDB descriptor is a table, not a string, so every single `/sql` query has been a hard 500 (`bad argument #1 to 'open' (string expected, got table)`) on the real production database since task #82's cutover. Found while checking on `document_embedding` row counts in production for an unrelated question -- no test ever exercised `/sql` against the MariaDB backend to catch it. `db.query` already dispatches correctly to either backend and already returns `(rows, column_names)` for both (confirmed directly in `luam/lib/database.lua`) -- `run_adhoc` just never used it. Both vendored query functions throw via `error()` on invalid SQL rather than returning `nil, err`, so the fix `pcall`s the call to keep converting a typo'd ad-hoc query into this page's own inline error message instead of a generic 500. New test in `mariadb_backend.bats` (skips without a live MariaDB server, as that whole file already does, but documents the exact regression). Also grounded the agent's own answers in real page content: found that `document.search`'s tool result returned only `"#id title"` lines even though `document.search` itself already fetches full content for scoring -- the agent could find which pages might be relevant but never actually read one before answering. Now returns a bounded (1200-char, word-boundary-trimmed) excerpt per result. Not yet pushed/redeployed -- same as every other fix this session, needs a push via VSCode first.
- 87: Persist full AI prompts/reasoning in the Knowledge Pool. Scoped against the actual source Fossil-fork system (`~/projects/fossil-scm/doc/ai/`, `src/ai.c`) rather than designing from scratch -- found two tables this port never carried over at all: `ai_context` (prompt/reasoning/token persistence, tied to a commit in the source system) and `ai_chat_eval` (per-reply classification + user feedback), both now adapted as `knowledge_context`/`knowledge_chat_eval`, tied to `agent_message` instead of a commit -- this is an immutable, append-only audit store, not version control, so a chat turn's own assistant-response row is the natural anchor, not a Fossil-style checkin. Five real, separate pieces of work, confirmed by explicit user direction: (1) **`knowledge_context`**: the exact `system_prompt` + assembled history actually sent to the model each turn (compaction's own summarization call too, not just chat replies), plus real token counts -- `agent_provider_vertex.lua` now parses Vertex's own `usageMetadata` instead of only ever having `agent.estimate_tokens`' char/4 heuristic available; the deterministic test provider returns matching estimated counts so the same code path is exercised under tests, never nil. A reply leaking visible reasoning (`<think>` tags, "Thinking..." prefixes, same detection as the source system's `ai_chat_eval_has_visible_reasoning`) gets that reasoning split into its own `knowledge_note` (`source_type='reasoning'`) rather than a second parallel log -- it goes through the exact same tiering/retrieval/decay pipeline as everything else. (2) **`knowledge_chat_eval`**: every reply classified (final/reasoning-visible/error/empty) plus the user-feedback half explicitly requested -- thumbs up/down on each assistant reply in the chat widget, `/api/chat-widget-feedback`, ownership-checked the same way every other chat-widget route already is (ties a message id back to its session, verified live: a second user submitting feedback against someone else's message id gets a 404, not a silently-accepted write). (3) **Heat decay**, explicit user direction ("knowledge should stay fluid, not settle into a fixed state"): neither this port nor the source system ever had this -- heat only ever grew. Computed lazily (`knowledge.effective_heat`, an exponential half-life of 14 days) wherever heat drives a decision, not a scheduled job -- this codebase has no in-app background scheduler at all. `knowledge.promotion_target_tier` is now genuinely bidirectional -- recomputed fresh from current retrieval_count and *decayed* heat every review instead of only ever ratcheting upward from the note's existing tier, so a note that stops being retrieved actually drops back down; verified directly with a manually-seeded tier-3 note whose `last_retrieved_at` was 120 days in the past (effective heat 3.0 -> 0.01) and confirmed the math, plus a dedicated unit test for the demotion path itself. (4) **Note materialization** (`knowledge.materialize_note`): the "durable artifact" layer the source system's own design describes but this port never built -- `promote` used to be just a tier number change. A tier 2/3, non-duplicate note becomes a real Notebook page (`document.create_page`), tracked back via new `artifact_document_id`/`artifact_status` columns so it's never materialized twice -- and per explicit user direction ("of course there should be a user approval for edits, so the agent won't go about and change willy nilly"), this is the one destructive `AGENT_TOOLS` entry in the whole knowledge namespace: the agent can propose it, but nothing is written until a human approves the resulting pending action, exactly like `entity.create`/`document.update` already work. (5) **Agent-driven review** (`agent.run_knowledge_review`, `platform knowledge review`): explicit user direction to use the agent itself to review retrieval/answer quality, not just the existing rule-based pass -- an ordinary chat session under the hood (its own system prompt instructing the model to use the new `knowledge.list` read tool and judge which notes are genuinely ready, conservatively), so a proposed materialize pauses for approval exactly like any user-initiated chat does. Dispatched from `main.lua` directly rather than `knowledge.do_knowledge` itself -- `knowledge.lua` can't require `agent.lua` back (agent.lua already requires knowledge.lua; a real circular require, not just an ordering nuisance). Found and fixed four real, pre-existing bugs along the way, none hypothetical: `agent.add_message` and `knowledge.create_note` both re-derived their new row's id via `SELECT MAX(id)`, the exact same real concurrent-CGI race already fixed in `ledger.lua` under task #77 -- now both use `db.exec`'s own connection-scoped insert id, needed correctly now that `knowledge_context`/`knowledge_chat_eval` key directly off `agent_message.id`; a schema field named `name` collides with `schema.lua`'s own builtin `name` column, previously failing with a raw "duplicate column name" SQL error -- added a reserved-field-name check to `schema.validate` for a clear message instead; `knowledge_note.duplicate_of` comes back as `""`, not Lua `nil`, for a genuinely NULL column under this sqlite binding -- a bare `!= nil` check in `materialize_note` incorrectly treated every never-duplicated note as a duplicate until this was found live and fixed; `sqlite_update` and `mariadb_update` disagree on what their own first return value even means (plain `true` vs. a real affected-row count) -- confirmed directly in `luam/lib/database.lua`, which is exactly why `knowledge.record_chat_feedback` checks existence with a real `SELECT` first instead of trusting that value, matching why every other UPDATE call site in this codebase already ignores it too. Full test suite green via `bld/test.sh` (both the Luam unit suite run through the real interpreter and the bats integration suite, 114 bats tests + all unit tests, exit 0) -- new coverage: 8 new unit tests in `tst/unit/knowledge.lua` (decay math, reasoning detection, reply classification, the demotion path) and a new `tst/integration/knowledge_context.bats` (real prompt/token persistence, reasoning-note linkage, a provider-failure path confirmed to record as an error without ever reaching a real model call, materialize's tier/duplicate/double-materialization refusals, the full review-to-pending-approval path, and chat feedback's ownership check).
- 103: Second redeploy of `fossci-app-prod`, after `platform-wip`'s `main` (task #89's Settings UI, config scaffolding, and the `/login` theme fix) was pushed to origin. Hit a real, separate infra problem along the way: the VM's boot disk was completely full (49G/100% used) -- 45 accumulated Docker images (46.6GB, 95% reclaimable) from every past `--redeploy` ever run, since nothing prunes old image tags. This silently killed the deploy partway through (`gcloud`'s own crash log showed `sqlite3.OperationalError: database or disk is full`), leaving the startup script's log stalled mid-`docker pull` while dockerd's own `restart: always` quietly kept the *previous* containers running -- looked like a successful redeploy (containers "up", real login working) but was still serving the pre-#89 image. Freed via `docker image prune -a -f` (46.6GB to 42GB free, 13% used); worth a follow-up task to add pruning to the deploy/startup flow so this doesn't recur. Completed the redeploy manually from there (pull + `docker compose down/up`, then the Step 9a seed-content refresh: schemas/extensions/views/templates/theme). Verified end-to-end: `/login` now renders with the real Celleste theme (`fossci-accent: #C97F1E`) and favicon link (task #89's fix, confirmed live); the Settings page is reachable from `/system`; real data still intact (`/browse?type=sample` still shows 100 of 77230 post-redeploy). Also reset the admin password to a real, randomly generated one (the placeholder set while diagnosing #102 no longer works, confirmed with a 401).
- 102 (critical, found live 2026-07-22): `fossci-app-prod`'s real web traffic had been completely unable to log in since task #82's MariaDB cutover -- not a credentials problem, a config gap. `elab/schema/platform/apache-platform.conf`'s `PassEnv` line was never updated when task #82 introduced `PLATFORM_DB_BACKEND`/`PLATFORM_MARIADB_*` (same class of gap `VERTEX_PROJECT` hit once before, per that line's own pre-existing comment) -- mod_cgid doesn't forward the container's environment into CGI scripts by default, only variables named there. Confirmed directly: `docker exec` (which does inherit the full container env) correctly talked to the real Cloud SQL database the whole time, but every real HTTP request through Apache read `config.db_backend()` as unset, silently fell back to SQLite, and `cgi.lua`'s own per-request auto-init quietly created a second, schema-correct but completely empty store at `/data/.store/store.db` -- 23 entity types, zero rows, including zero `user` rows. Every login attempt through the real site 401'd because the *website's own* `user` table had always been empty, even though `docker exec ... user add` had already created real accounts in the actual MariaDB database, invisible to the site the whole time. Fixed by adding the missing vars to `PassEnv`; rebuilt via Cloud Build (`elab/schema/platform/deploy.sh --redeploy`) and reset `fossci-app-prod`. Verified end-to-end post-redeploy: `docker exec platform env` shows `PLATFORM_DB_BACKEND=mariadb`; a real login through the live site (not `docker exec`) succeeds; `/data` reports 23 registered entity types; `/browse?type=sample` through a real authenticated session shows **100 of 77230** -- the real migrated production data, reachable through the actual website for the first time since the cutover. Cleaned up the now-inert stray SQLite file and a temporary diagnostic test user afterward. Not yet done: this redeploy's image still has platform-wip's *previous* `main` (the PassEnv fix lives in `~/software`, unaffected) -- task #89's own fixes (the `/login` theme bug found live during this same investigation, the new Settings UI, config scaffolding) are committed locally but not yet pushed to `platform-wip`'s `origin/main` (git CLI push isn't authenticated in this environment; needs a push via the VSCode git integration), so a second rebuild+redeploy is still needed once that's pushed to bring those in too.
- 89: Two real, separate sub-features, per follow-up user direction on scope. (1) **Settings UI**: a new `/settings` page (Admin-capability-gated, matching `/admin-users`'s own gate) covering every `theme.json` field -- `site_name`, `hide_home_heading`, `system_prompt_extra`, the 12 color overrides, and logo/favicon uploads -- backed by a new `config.save_theme()` (symmetric to the existing `load_theme`). Deliberately does NOT cover env-var-driven config (DB backend, agent provider/model, Vertex project/region) -- those are process-bootstrap values read once at CGI-process start, unsafe to change from inside a running request. Logo/favicon upload needed real new plumbing this codebase didn't have at all: a minimal `src/multipart.lua` (RFC 7578 multipart/form-data parser, just enough for a file field alongside text fields -- no existing file-upload path existed anywhere in the app before this). Found and fixed a real bug in my own first version of the parser (boundary-position off-by-one silently swallowed the next boundary marker into the current field's content) via a byte-level Python simulation before touching the real Lua. Color values are validated against a strict allowlist before being written anywhere -- they get embedded straight into a `<style>` block by `html.theme_root_css`, not HTML-escaped since it isn't HTML content, so an unvalidated value could break out of `<style>` as stored injection against every visitor, not just whoever filled in the form (verified directly: a value containing `</style><script>` is rejected with a clear error, never reaches theme.json). Uploaded files are checked against real PNG magic bytes (the `accept="image/png"` attribute is only ever a client-side hint) and written under fixed destination filenames, never the client-supplied one (same "never path-build from user input" reasoning as the existing `/theme-asset` allowlist). (2) **Config-as-code scaffolding**: `platform init --with-examples` (opt-in, not the default -- a plain `platform init` stays the truly empty, generic install task #101 verified and fixed) generates one real, working example of each kind (an entity schema exercising every field type including a reference, a matching view, an extension with a before-hook, an entry template) plus a fully-populated `theme.json` stub, all heavily commented. Safe to re-run against an existing store -- skips any destination that already exists rather than overwriting it, verified directly (deleted one example, kept the rest, re-running regenerated only the missing one and left a hand-edited file untouched). Found and fixed a real, previously-latent bug while writing the example schemas: a field named `name` collides with `schema.lua`'s own builtin `name` column, failing at `schema add` time with a raw "duplicate column name" SQL error -- added a reserved-field-name check to `schema.validate` so this is now a clear, friendly error instead, for any schema author, not just this example. Also fixed, found live while testing on the real `celleste-lims.com` deployment during this task (unrelated to either sub-feature above, but a real, user-visible bug hit along the way): `/login` printed `html.render_login`'s bare content fragment directly instead of going through `html.page_shell` like every other route -- the one page every user sees before authenticating had no `<head>`, no theme `:root{}` CSS block, and no favicon `<link>` at all, confirmed live (a deployment's real site name/colors never applied to the login page, and the browser tab fell back to a stale cached favicon from a previous origin). Full test suite green (`./bld/build.sh` + `bats tst/unit tst/integration`, 105 tests, exit 0), including new coverage: `tst/integration/settings.bats` (Admin-gating, CSRF, color-injection rejection, real-vs-fake PNG upload), `tst/integration/init.bats` (empty-by-default, `--with-examples` end to end, re-run safety, the reserved-field-name error), and a new case in `tst/integration/auth.bats` for the `/login` page-shell fix.
- 94: Evaluated a tamper-evidence/immutability layer for the ledger -- verified against the real code and real production access controls, not just the task's own original framing. Confirmed directly: `entity_event` has exactly two `UPDATE` statements in the whole codebase (`ledger.lua`'s `append_create`/`append_archive`), and both are immediate post-insert fixups completing a just-inserted row's own `entity_id`/`event_type` before any concurrent reader could observe the intermediate state -- not retroactive edits to an already-observable row. The append-only invariant genuinely holds in application code. The real threat is a direct database connection bypassing the app entirely (documented in `~/software/docs/platform-direct-database-connectivity.md`, found during task #99): only one such credential exists (the full-access `platform` MySQL user), gated by `roles/cloudsql.client` IAM + a Cloud SQL Auth Proxy tunnel, password in Secret Manager, zero public network exposure on the instance itself -- and a direct write is invisible to `entity_event` forever, confirmed the hard way when task #99's own testing accidentally deleted 2 live rows (recovered only via Cloud SQL's point-in-time clone, not any ledger-level protection). Recommendation if this is ever built: HMAC-sign each row (`row_signature = hmac(secret, event_id .. entity_id .. entity_type .. event_type .. field_changes .. created_at)`), reusing `auth.lua`'s own session-signing pattern (a locally-generated secret, `hmac.sha256`, never transmitted) rather than a new primitive -- and deliberately keep that signing secret out of Secret Manager, since the whole point is that it must stay unreachable to whoever holds the `platform-db-password` credential (a different access path: Auth Proxy + IAM, not container/host filesystem access). Plain hash-chaining alone was considered and rejected as the primary mechanism -- without a secret key, anyone with the write access this is meant to guard against can simply recompute the chain forward after an edit; HMAC signing is the meaningfully stronger version of the same idea at comparable cost. Not implementing now: the one demonstrated real-world failure to date was operator error (an over-privileged grant), not deliberate tampering, and Cloud SQL PITR already recovered it fully; adding signing now means a schema migration against live production data plus a real key-distribution and verification-job design for a risk that hasn't materialized and isn't yet a compliance requirement. Revisit if GLP/21 CFR Part 11 certification is ever pursued, or if a second direct-DB-write credential is ever created (widening who could tamper undetected beyond today's single, tightly IAM-gated one).
- 101: Audited platform-wip running fully unmodified (no Celleste-specific config) for leaked Celleste-specific assumptions. Source-level pass: clean -- every "Celleste" mention in `src/*.lua` is a comment explaining the generic/overlay split, never a literal in an actual code path (one test fixture in `tst/unit/render.lua` uses "Celleste & Co" as example data, harmless). Live check: built the binary, ran a fresh `platform init` in a directory with no `theme.json`/`schemas/`/`extensions/`/`views/`/`templates/` seeded, logged in, and rendered Home/System/Data. Found one real, concrete leak: the nav-rail "Tasks" icon and Home's matching quick-link both unconditionally pointed at `view?view_name=prioritized_tasks` (`src/html.lua`), a Celleste-authored saved view -- on the generic install this 404'd with a raw internal path (`cannot open view: ./views/prioritized_tasks.lua`), unlike the System link and logo slot, which were already properly gated. Fixed: `cgi.lua` now checks once whether `views/prioritized_tasks.lua` actually loads (`has_tasks_view`) and threads that through to both `html.page_shell` and `html.render_home`, the same pattern `show_sql_nav`/`show_admin_nav`/`theme.has_logo` already use. Verified both ways: the generic instance now shows no Tasks link at all, and a second instance seeded with the real `elab/schema/platform` config (schemas/extensions/views/templates/theme.json copied in) still shows a working Tasks link. Full test suite green after the fix (`./bld/build.sh` + `bats tst/unit tst/integration`, exit 0). Coherence check (part 2): a generic install boots to a real, usable "Platform" home page, `/data` correctly reports "1 registered" entity type (the built-in Pages/document type, not zero), and `/system` shows working generic-only features (Knowledge Pool, SQL console, Users, Templates) -- genuinely coherent standalone, not just "technically boots." Adjacent, not fixed (out of this task's literal scope): pervasive `fossci-*`/`fossil-doc` CSS class names throughout the generic UI are leftover naming from the predecessor system this was migrated from (tasks #39-44) -- cosmetic, never user-visible, no functional impact, but a leaked assumption from history baked into otherwise-generic markup, worth a future pass if it's ever worth the churn.
- Dropped 45 (extract escape helpers into `luam/lib/escape.lua`) -- user confirmed the underlying problems it targeted are already resolved by two separate `luam` changes: `lib/database.lua`'s `local_query`/`local_update` now escape string arguments automatically (no more scattered manual `escape_sqlite` calls to consolidate into a shared helper), and `lib/string_utils.lua`'s new `template`/`template_named` `{{}}`/`{{key}}` substitution replaced `string.format` for CSS/JS/HTML templating (the `%`-escaping collision problem a dedicated escape-helpers module would otherwise still need to solve). No dedicated module needed anymore.
- 98a: Gave the Benchling-sync Cloud Run job's integrity reports real history. Correction to how this was originally scoped: `run_integrity_checks()` already wrote a JSON report by itself (`resolve_defaults()` fills in a real path even through the cron job's own `arg_overrides` mechanism, confirmed by reading `post_build_integrity.py` directly) — a report was never actually missing. The real gap: that path was identical on every run, so each day's report overwrote the last, both locally and on OneDrive (`sync_to_onedrive`'s `rclone copy` just replaces the same file path) — no history ever survived. Fixed with a per-run timestamped filename (`logs/integrity-reports/integrity-<timestamp>.json`, matching `backup_database()`'s own naming convention). `fail-on-warning`/`strict-plan`/`skip-backup-compare` all default to `False` in both entry points already — not an actual parity gap, left alone rather than silently changing cron-job policy.
- Reverted task #56 (pre-push git hook running the full test suite), in both this repo and `luam` — explicit user direction: it's genuinely redundant with tests already being run manually before most pushes this session, the hook has no server-side backup (neither repo has any GitHub Actions CI), and a properly-async CI setup is deliberately deferred until there's a real test/stage environment to enroll changes into more structurally — not something to bolt onto the git-workflow level piecemeal right now. Removed `githooks/pre-push` (and the now-empty `githooks/` directory) from both repos, unset `core.hooksPath` locally, and dropped the stale reference from this repo's own `doc/architecture.md`.
- 99: Added `elab/schema/platform/connect-platform-db.sh` (Auth Proxy + `mysql` client, cached binary, cleans up its own tunnel on exit) plus a real `platform_readonly` MySQL user (`SELECT` only) it defaults to, with `--admin` for the app's own full-access user. Found and fixed a serious bug while testing: Cloud SQL for MySQL auto-grants every new user the `cloudsqlsuperuser` role regardless of explicit `GRANT`s — confirmed when the "read-only" user successfully ran a real `DELETE`, taking out 2 live `sample` rows before the bug was caught. Recovered in full via Cloud SQL's own point-in-time clone (cloned to a pre-incident timestamp, diffed ids, re-inserted the exact 2 rows via `mysqldump`, verified all 39 tables match) — `cloudsqlsuperuser` explicitly revoked from `platform_readonly` and reverified (`SHOW GRANTS` + a real failed `DELETE` attempt). Both the role-grant gotcha and the fact that direct writes never touch the `entity_event` ledger (no audit trail at all) are now documented prominently in `~/software/docs/platform-direct-database-connectivity.md` and the script's own header comment.
- 90: Repo housekeeping in `~/software`'s `elab/` tree — deleted the abandoned bootstrap Terraform root (`infra/gcp/celleste-elab-services/`, confirmed still broken/incompatible before removing, and confirmed its dangerous part — the local `terraform.tfstate` independently managing the same live `fossci-app-prod` instance — was never git-tracked, so deleting it fully defuses the landmine); added a prominent banner + corrected repository-layout diagram to `elab/README.md` distinguishing what's actually deployed (`schema/platform/`, `infra/environments/fossci-prod/`, `infra/modules/fossci_compute/`) from the separate, not-yet-built eLabFTW design series the rest of that README describes (previously gave zero indication these were different systems); removed a stale, already-gitignored local artifact (`entries_raw.json`).
- 100: Decommissioned the old SQLite file on `fossci-app-prod`'s VM. Used `sqlite3 .backup` (not a raw file copy) to get a consistent snapshot -- found directly, mid-task, that the database is in WAL mode, so even read-only diagnostic queries were causing physical-byte (not logical-content) drift between checks; a raw `cp`/`gsutil cp` of the main db file would have been the wrong method regardless of freshness. Verified all 39 tables' row counts matched the original migration exactly on the final backup before archiving it. Uploaded to a new bucket (`gs://celleste-elab-fossci-rollback`), byte-verified via MD5 (bucket write access granted to the VM's SA temporarily for the one-time upload, then revoked). Confirmed `session_secret` (same directory, but an unrelated live HMAC key for session/CSRF signing) was left untouched, and the live site + a real entity-count check (77,230 `sample` rows) both stayed correct after deleting `store.db` from the VM's local disk.
- 97: Cleaned up overly-broad default VPC firewall rules in `celleste-elab` — `default-allow-ssh`/`default-allow-rdp` (0.0.0.0/0) and the orphaned `allow-elab-http` (target tag matched zero real instances, confirmed via `gcloud compute instances list` before deleting) all deleted. SSH replaced with `allow-ssh-iap-only` (35.235.240.0/20, IAP tunnel only) — verified `gcloud compute ssh --tunnel-through-iap` works both before and after the cleanup, and the live site stayed up throughout. New rule tracked in Terraform (`environments/fossci-prod`) via `terraform import`, not left as an unmanaged snowflake resource — confirmed `terraform plan` shows no drift.
- 82: MariaDB migration Phase 4 (cutover) — `fossci-app-prod` now runs against Cloud SQL for MySQL (`platform-db-prod`) via a Cloud SQL Auth Proxy sidecar, not SQLite. Real MySQL (not MariaDB) surfaced 5 previously-latent dialect gaps (see `doc/mariadb-migration.md` for the full list: TEXT-column defaults, missing `CREATE INDEX IF NOT EXISTS`, the `rank` reserved word, arbitrary schema field names needing `db.quote_ident`) plus 5 more real bugs found migrating ~307k rows of real production data via `LOAD DATA LOCAL INFILE` (escaping, NULL-vs-empty-string, charset, collation-ignorable control-byte comparisons — full writeup in the migration doc). All 39 tables verified byte-exact (numeric-epsilon for `REAL` columns) before cutover; live post-cutover check confirmed the running app serving the real migrated data (`entity list sample` = exactly 77,230 rows). SQLite file + snapshot policy kept untouched as a rollback path. Also found (unrelated, left alone, folded into task #90 above): an old abandoned bootstrap Terraform root independently manages the same live compute instance as the current one.
- 64: `[[title]]` links now render as a styled widget in Toast UI's WYSIWYG mode, via the editor's own `widgetRules` API — purely cosmetic while editing; resolved-vs-dangling status is still computed server-side on save/view, unchanged
- 93: "Reason for change" field for the ledger's update/archive events — nullable `entity_event.reason` column; per-schema opt-in via `require_reason_on_update`/`require_reason_on_archive` type-level flags (`entity.update`/`entity.archive` reject the change when required and missing); wired into `/api/update`/`/api/archive` (plain query/form param) and the chat agent's `entity.update` tool; reason renders on `/detail`'s history. Note: only the touchpoints with a real caller today were wired (CGI routes, agent's `entity.update` tool) — `entity.archive` isn't an agent tool, and there's no separate web edit/archive form beyond the CGI routes, so no automated-sync-path default string was needed either.
- 70: Deployment-configurable `system_prompt_extra` — `theme.json` field appended verbatim to `agent.default_system_prompt`, matching fossil-scm's own prior art
- 91/92: Vertex AI + general lab-data compliance research done — see `~/software/docs/ai-agent-and-lab-data-compliance-research.md` (deliberately placed outside the separate `elab/docs/01-07` eLabFTW design series, which describes a different, not-yet-built system). No blockers found; surfaced two concrete future gaps (tasks #93, #94) and unblocked #87.
- 46: Fixed the 3 bare cross-file helper calls flagged by the build-time audit (luam's `gnuplot.lua`/`utils.lua`/`user.lua`) — two were real, fixed by calling through the module table properly; the third (`escape_string` in `user.lua`) turned out to be referenced only from commented-out dead code, deleted rather than "fixed"
- 83: Removed `/data`'s embedded SQL widget (persistent styling problem, explicit call to stop chasing it) — `/sql` itself untouched, still reachable via `/system`
- 72: Chat history sidebar now shows each session's start timestamp under its title
- 71: Sessions with no explicit title now get one auto-generated from their first real message (reuses `knowledge.guess_title_from_body`)
- 81: MariaDB migration Phase 3 — every SQLite-dialect DDL/DML token routed through new `db.lua` helpers; found and fixed two dialect gaps a live server surfaced (TEXT-as-key needs `VARCHAR(255)`, TEXT-in-index needs a prefix length); `bld/build.sh` now conditionally compiles MariaDB support into the binary itself; luam gained `CLIENT_MULTI_STATEMENTS` support. `platform init` + full entity CRUD + the concurrent-create race check all verified end-to-end against a live MariaDB server; automated bats coverage added (`tst/integration/mariadb_backend.bats`)
- 80: MariaDB migration Phase 2 — `src/db.lua` dispatches by `db_path`'s shape (string vs. descriptor table), `db.quote`/`db.literal` kept backend-agnostic via `NO_BACKSLASH_ESCAPES`, `config.lua` gains `db_backend()`/`mariadb_descriptor()`. Verified directly against a live server (query/exec/quote/table_exists/get_tables/get_columns); `platform init` itself still needs Phase 3's dialect migration before it can succeed against MariaDB
- 77: Fixed `entity_id`/`event_id` race in `ledger.lua`'s create/update path — `SELECT MAX(event_id)` replaced with `db.exec`'s connection-scoped `last_insert_rowid()` return value; added a concurrent-load integration test (see `tst/integration/entity.bats`)
- 79: MariaDB migration Phase 1 — persistent-connection wrapper in luam's `lib/database.lua` (`mariadb_query`/`mariadb_update`, one cached connection per descriptor, not open-per-call)
- 78: MariaDB migration Phase 0 — new `lib/mariadb/lmariadb.c` binding in luam (connect/query/exec/escape/ping/close), verified end-to-end against a real local MariaDB server
- 69: Knowledge Pool Phase 3 — landing page, nav revert (removed dedicated "Chats" rail icon, `/chat` reachable via System → Knowledge Pool)
- 68: Knowledge Pool Phase 2 — lazy note creation, agent tool, CLI (`platform knowledge stats/list/show/promote`)
- 67: Knowledge Pool Phase 1 — wire retrieval logging into `document.search`
- 66: Knowledge Pool Phase 0 — schema + pure logic module (`src/knowledge.lua`)
- 63: Deploy Toast UI Editor integration to production
- 62: Restore "knowledge pool" browsing (chat history / retrieved info) from old fossci
- 61: Add "start new chat" control to chat widget
- 60: Diagnose and fix silent agent-response failure in chat widget
- 59: Fix chat widget resize handle to grow from top-left, not bottom-right
- 56: Add pre-push git hook running the test suite
- 55: Fix deploy metadata-drift gap in deploy.sh/terraform
- 54: Build autoescape-by-default template helper
- 52: Persist chat widget open/closed state across page navigation
- 51: Make the chat widget dynamically resizable
- 50: Clean up dirty/raw output in chat transcript
- 49: Add a working/thinking indicator to the chat widget
- 48: Build a real rich-text (WYSIWYG) editor for Pages
- 47: Deploy real Celleste logo/favicon assets to production
- 39-44: Initial fossci → platform-wip migration (schema/template/view/extension port, Pages migration script, admin user setup, Docker/deployment config, real redeploy + end-to-end verification)

## Recently fixed production bugs (not tracked as tasks — already resolved)

- Universal current-user/current-page context for the chat agent (no owner awareness / auto subject / default due time when creating entities via chat).
- `/sql?embed=1` losing theme styling twice: first missing the `:root{}` theme-variable block entirely, then (after that fix) the block landing in the wrong template placeholder and rendering as visible page text instead of being applied as CSS.
- Home page company logo 404 in production, caused by a `cp -r` nesting bug in `startup-platform.sh.tpl`'s theme-assets copy step.
- Chat widget silently swallowing fetch errors (thinking indicator would disappear with no message sent and no feedback).
- `static/` → `vnd/` rename leaving stale cross-repo references in `~/software`'s Dockerfile/apache config/startup script, causing a real Cloud Build failure.
